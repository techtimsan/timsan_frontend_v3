import { HeroCarouselCardProps } from "@/types/app"
import { Card, CardBody } from "@nextui-org/card"

const HeroCarouselCard = ({}: HeroCarouselCardProps) => {
  return (
    <Card>
      <CardBody></CardBody>
    </Card>
  )
}

export default HeroCarouselCard
